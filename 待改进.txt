1、对于
\[
\mathcal{L}_{\mathrm{conf}} = \sum_{(\hat{x},c)\in (\hat{x},\mathcal{C})}\left(c\cdot \left\Vert \frac{\hat{x}}{\hat{s}} -\frac{x}{s}\right\Vert_{2} - \alpha \log c\right), \quad (7)
\]
求和符还是要高出右边求和符的求和项公式一截呀，要高出图片中上面那种高度效果，且花体不要太花，也跟上面公式一样

3、高度变小了，可以，但运行按钮太窄了，搞高点（和上面的浏览按钮一样高）

4、对下面这个markdown
“
表2总结实验结果，并对比文献中其他模型架构的翻译质量与训练成本。训练浮点运算量通过训练时间、GPU数量与单GPU持续单精度浮点算力估算⁵。

表3第A行：保持计算量不变，调整注意力头数与键值维度（见3.2.2节）。单头注意力比最优设置低0.9个BLEU值，头数过多效果也会下降。

表3第B行：减小注意力键维度 \(d_{k}\) 会降低模型效果，说明兼容度计算难度较高，比点积更复杂的兼容函数可能更优。第C、D行：如预期所示，更大的模型效果更好，Dropout对防止过拟合效果显著。第E行：用可学习位置嵌入[8]替代正弦位置编码，效果与基础模型几乎一致。

**表3**：Transformer架构变体。未列出参数与基础模型一致。所有指标基于英德翻译验证集newstest2013。困惑度按字节对编码的词片计算，不可与单词困惑度直接对比。
”
只有“表3：”这总格式才是真正的表题注文字（*等斜体或加粗不能影响判断，图题注一样的原理），且判断为题注的需要居中，好像还没实现居中呀

